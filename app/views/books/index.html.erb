<% if @book.errors.any? %>
    <%= @book.errors.count %>errors prohibited this obj from being saved:
    <% @book.errors.full_messages.each do |message| %>
      <ul>・<%= message %></ul>
    <% end %>
  <% end %>


<div class="container">
  <div class="row">
    <div class="col-xs-3">
      <%= render 'users/user', user: current_user, book: @book %>
    </div>

      <p id="notice"></p>

    <div class="col-xs-9">
      <h2>Books</h2>

      <table class="table table-hover table-inverse">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
         <tr>
          <% @books.each do |book| %>
            <td>

             <%= link_to attachment_image_tag(book.user, :profile_image, :fill, 40, 40, class: "pull-left profile-img", fallback: "none.png", size:'40x40'), user_path(book.user) %>
            </td>

            <td>
             <%= link_to book.title, book_path(book) %>
            </td>


            <td>
              <%= book.body %>
            </td>
          </tr>
        </tbody>
          <% end %>
      </table>
    </div>
  </div>
</div>

